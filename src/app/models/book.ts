export class Book {
    
    public id_book:number
    constructor(public title: string, public type: string, public author: string, public price:number, public photo: string, public id_user: number){

    }
}
